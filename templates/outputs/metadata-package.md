# Upload Metadata — `{WORKING_TITLE}`

> Fill in OUTPUT LANGUAGE. Section headings may be translated.

Everything below is **copy-paste ready** — no commentary inside the blocks.
**Video:** `{SLUG}` · **Duration:** `{MM:SS}` · **Publish:** `{DATE}` `{TIME}` `{TIMEZONE}`

## 1. Title

```
{FINAL_TITLE}
```

| Check | Value |
|---|---|
| Characters | `{N}` / 100 |
| Primary keyword ends at char | `{N}` (must be ≤ 50) |
| Target traffic source · length appropriate | `{SEARCH/BROWSE}` · `{YES/NO}` |
| Contains a number | `{YES/NO}` |

## 2. Description

```
{FULL_DESCRIPTION — one block, exactly as pasted into Studio, including chapters,
links, timestamps and hashtags in their final positions}
```

**First 150 characters, extracted for verification:**

```
{FIRST_150_CHARS}
```

| Check | Value |
|---|---|
| Total characters | `{N}` / 5,000 |
| Body word count | `{N}` (target 200–350) |
| Keyword in first 25 words · count in body | `{YES/NO}` · `{N}` (target 2–4) |
| Links included | `{N}` |

## 3. Chapters

Must start at `0:00`, minimum 3, minimum 10 seconds each.

| # | Timestamp | Chapter title | Length |
|---|---|---|---|
| 1 | `0:00` | `{TITLE}` | `{MM:SS}` |
| 2 | `{MM:SS}` | | |
| 3 | | | |

## 4. Tags

```
{TAG_1}, {TAG_2}, {TAG_3}, {TAG_4}, {TAG_5}
```

Tag count: `{N}` · total characters: `{N}` / 500

## 5. Hashtags

```
#{HASHTAG_1} #{HASHTAG_2} #{HASHTAG_3}
```

The first three display above the title. Exceeding 15 makes YouTube ignore all of them.

## 6. Pinned comment

```
{PINNED_COMMENT_TEXT}
```

Job it does: `{QUESTION / CORRECTION / LINK / SOURCES}` · posted immediately on publish.

## 7. End screen and cards

| Element | Timestamp | Points to | Why this one |
|---|---|---|---|
| End screen — video | `{MM:SS}` | `{VIDEO}` | `{REASON}` |
| End screen — subscribe | `{MM:SS}` | — | — |
| Card 1 | `{MM:SS}` | `{VIDEO/PLAYLIST}` | `{REASON}` |
| Card 2 | `{MM:SS}` | | |

End screen occupies the last 15–20 seconds. Confirm the script leaves room for it.

## 8. Pre-publish checklist

- [ ] Title pasted and character count confirmed in Studio
- [ ] Description pasted; first 150 characters verified in the collapsed preview
- [ ] Chapters render as chapters in the Studio preview (not plain timestamps)
- [ ] Thumbnail uploaded at 1280×720 or larger, within the file-size limit
- [ ] Captions uploaded or auto-captions reviewed for the primary keyword
- [ ] Playlist(s) assigned: `{PLAYLISTS}`
- [ ] Category, language, "made for kids" set · paid-promotion disclosure toggled if applicable
- [ ] End screen and cards placed
- [ ] Scheduled for `{DATE}` `{TIME}` `{TIMEZONE}`, pinned comment ready to post
- [ ] Shorts cutdowns queued: `{N}`

## Self-check

- [ ] Title within 100 characters; keyword and hook inside the first 40–50 (`benchmarks.md` §6)
- [ ] Description under 5,000 characters; first 150–200 carry the hook and primary keyword
- [ ] Keyword appears in the first 25 words and 2–4× in the body
- [ ] Chapters start at `0:00`, at least 3, each at least 10 seconds
- [ ] Tags total 500 characters or fewer
- [ ] Exactly 3 hashtags listed; total never approaches the 15 limit
- [ ] End screen sits in the last 15–20 seconds and the script accommodates it
- [ ] All text in OUTPUT LANGUAGE and written for that market, not translated
- [ ] Every block is copy-paste clean — no placeholders or notes left inside
- [ ] Disclosure requirements checked (`benchmarks.md` §9 FTC disclosure, plus local rules)
