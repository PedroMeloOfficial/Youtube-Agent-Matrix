# Idea Cards — `{TOPIC_OR_BATCH}`

> Fill in OUTPUT LANGUAGE. Section headings may be translated.

**Date:** `{DATE}` · **Archetype:** `{PRIMARY}` · **Research dossier:** `{PATH}`
Six cards. Each is pitchable on its own — the creator reads one and can say yes or no.

---

## Card `{N}` — `{WORKING_TITLE}`

*(repeat this whole block for each of the 6 ideas)*

| Field | Value |
|---|---|
| Working title | `{TITLE}` |
| Pillar | `{P1–P5}` |
| Format · target length | `{FORMAT}` · `{MIN}` min |
| Difficulty | `{LOW / MEDIUM / HIGH}` — `{WHAT_MAKES_IT_HARD}` |

**Thesis — one sentence:**
> `{THE_SINGLE_CLAIM_THE_VIDEO_DEFENDS}`

**Hook — the literal first line, as spoken:**
> "`{FIRST_LINE_WORD_FOR_WORD}`"

Framework: `{NAMED_FRAMEWORK}` (`references/hook-library.md`)

**Promise — a capability, not a topic:**
> After watching, the viewer can `{DO_WHAT}`.

**Differentiation:** `{WHY_THIS_IS_NOT_THE_VIDEO_THAT_ALREADY_EXISTS}`
Closest existing video: `{TITLE_AND_CHANNEL}` — differs by `{DIFFERENCE}`

**Timing:** `{EVERGREEN}` / `{DATED — PUBLISH BY {DATE}, BECAUSE {EVENT}}`

**Structure — 3 beats:**

| Beat | Claim | Evidence | Turn into the next beat |
|---|---|---|---|
| 1 | `{CLAIM}` | `{F#}` | `{TRANSITION}` |
| 2 | `{CLAIM}` | `{F#}` | `{TRANSITION}` |
| 3 | `{CLAIM}` | `{F#}` | `{LANDING}` |

**Evidence base:** facts `{F#, F#, F#}` from the dossier · b-roll confirmed obtainable: `{YES/NO}`

**Honest risk:** `{WHAT_COULD_MAKE_THIS_FLOP}` — mitigation: `{MITIGATION}`

---

## Ranking

Scored 1–5 per column. `Fit` = fit to positioning and pillars. `Demand` = evidence anyone is
looking for this. `Edge` = how hard it is for a competitor to make the same video.

| # | Working title | Fit | Demand | Edge | Effort (inverted) | Total | Note |
|---|---|---|---|---|---|---|---|
| 1 | `{TITLE}` | `{1-5}` | `{1-5}` | `{1-5}` | `{1-5}` | `{SUM}` | `{NOTE}` |
| 2 | | | | | | | |
| 3 | | | | | | | |
| 4 | | | | | | | |
| 5 | | | | | | | |
| 6 | | | | | | | |

---

## ▶ Recommendation

> **Make card `{N}` — `{WORKING_TITLE}`.**
>
> **Why:** `{ONE_LINE_REASON}`
> **What it costs:** `{EFFORT_AND_TIMELINE}`
> **Runner-up:** card `{N}` — pick this instead if `{CONDITION}`.
> **Do not make now:** card `{N}` — `{REASON}` (revisit when `{CONDITION}`).

---

## Self-check

- [ ] Six cards, each genuinely different — not one idea in six titles
- [ ] Every hook is a literal spoken line, not a description of a hook
- [ ] Every hook names a framework from `hook-library.md`
- [ ] Every promise is a capability the viewer gains, never a topic label
- [ ] Every thesis is one sentence and is arguable — a fact is not a thesis
- [ ] Each card names the closest existing video and how it differs
- [ ] Every beat carries evidence traced to the research dossier
- [ ] Target length sits inside the archetype's band (archetype template §3, `benchmarks.md` §3)
- [ ] Titles checked against the archetype's §4 patterns and the front-load window (`benchmarks.md` §6)
- [ ] Every card states an honest risk — no card is risk-free
- [ ] Recommendation made with a one-line reason and a runner-up condition
