# Script — `{WORKING_TITLE}` · Variant `{A/B/C}` `{NARRATIVE/INSTRUCTIONAL/ARGUMENTATIVE}`

> Fill in OUTPUT LANGUAGE. Section headings may be translated.

| Field | Value |
|---|---|
| Target duration | `{MM:SS}` |
| Word count | `{N}` (at `{RATE}` wpm for `OUTPUT LANGUAGE` = `{MM:SS}`) |
| Pillar | `{P#} {NAME}` |
| Archetype length band | `{MIN}`–`{MAX}` min — inside band: `{YES/NO}` |
| Spoilers present | `{YES/NO}` |
| Open loop | planted `{MM:SS}` → paid off `{MM:SS}` |
| Interrupt count | `{N}` (required: `{N}` per `benchmarks.md` §2) |
| Research dossier | `{PATH}` |

**Markers:** `[B-ROLL: …]` visual · `[INTERRUPT]` pattern break · `[EMPHASIS]` load-bearing line
· `[PAUSE]` air · `⚠️ SPOILERS` spoiler block.

## `{00:00}` COLD OPEN

`{FIRST_LINE_WORD_FOR_WORD — no greeting, no channel name}`

`[B-ROLL: {SHOT}]`
`[EMPHASIS]` `{THE_LINE_CARRYING_THE_OPEN}`

## `{00:XX}` PROMISE

`{THE_CAPABILITY_THE_VIEWER_GAINS, STATED_AS_A_SENTENCE}`

`[B-ROLL: {SHOT}]`

## `{00:XX}` OPEN LOOP

`{THE_QUESTION_LEFT_DELIBERATELY_UNANSWERED}` `[PAUSE]`

`[B-ROLL: {SHOT}]`

## `{MM:SS}` BEAT `{N}` — `{BEAT_NAME}`

*(repeat this block for each body beat — one claim, one piece of evidence)*

**Claim:** `{ONE_CLAIM}`

`{SPOKEN_TEXT}`

**Evidence:** `{F#}` from the dossier — `{THE_FACT_AS_SPOKEN}`

`[B-ROLL: {SHOT}]`
`[INTERRUPT]` `{WHAT_CHANGES: cut / graphic / location / tone}`

**Transition out:** `{ESCALATION / REVERSAL / CONSEQUENCE / DIRECT ADDRESS}` — `{THE_LINE}`

## ⚠️ SPOILERS — `{MM:SS}` *(delete this block if not applicable)*

**Spoken warning:** `{LINE}`
**On-screen card:** `{TEXT}` — `{DURATION}s`
**Chapter marker:** `{MM:SS} {CHAPTER_TITLE}`
**Spoiler-free viewers rejoin at:** `{MM:SS}`

## `{MM:SS}` TURN

The point where the video's central claim gets harder than anything before it.

`{SPOKEN_TEXT}`

`[EMPHASIS]` `{THE_TURN_LINE}` `[PAUSE]`
`[B-ROLL: {SHOT}]`

## `{MM:SS}` PAYOFF

`{THE_OPEN_LOOP_CLOSED_EXPLICITLY}`

`[B-ROLL: {SHOT}]`

## `{MM:SS}` SO WHAT — why this matters after the viewer closes the tab

`{SPOKEN_TEXT}`

`[EMPHASIS]` `{THE_LINE_THEY_QUOTE_IN_THE_COMMENTS}`

## `{MM:SS}` CTA — exactly one

`{THE_SINGLE_ASK, SPECIFIC_TO_THIS_VIDEO}`

Next video to send them to: `{TITLE}` — reason it follows: `{REASON}`

## B-roll shot list

| # | Timestamp | Shot | Source | Obtainable? | Substitute |
|---|---|---|---|---|---|
| 1 | `{MM:SS}` | `{SHOT}` | `{SOURCE}` | `{YES/NO}` | `{SUBSTITUTE}` |
| 2 | | | | | |

Every `NO` must have a substitute, or the beat is rewritten.

## Self-check

- [ ] Cold open has no greeting and no channel name
- [ ] Promise is a capability the viewer gains, not a topic
- [ ] Open loop planted in the first minute and explicitly paid off before the close
- [ ] The TURN's claim is harder than anything in the first half
- [ ] Every beat is one claim plus one piece of evidence traced to the dossier
- [ ] Every beat carries a `[B-ROLL: ]` cue confirmed obtainable by the research
- [ ] `[INTERRUPT]` count meets the archetype's required frequency (`benchmarks.md` §2, Pattern interrupts)
- [ ] Word count matches `target minutes × {RATE}`, ±10% — `{RATE}` being the speaking rate for
      `OUTPUT LANGUAGE` (`references/localization-guide.md` §7; ~140 wpm is the English baseline)
- [ ] Duration sits inside the archetype band and clears the mid-roll threshold if intended (`benchmarks.md` §3)
- [ ] Spoiler protocol complete: spoken warning + on-screen card + chapter marker
- [ ] Exactly one CTA, at the end, specific to this video
- [ ] Reads as spoken language in OUTPUT LANGUAGE, not translated English structure
- [ ] No unverified claim asserted as established — anything shaky marked `⚠️ verify`
