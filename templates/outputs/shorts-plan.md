# Shorts Plan — `{PERIOD_OR_SOURCE_VIDEO}`

> Fill in OUTPUT LANGUAGE. Section headings may be translated.

**Archetype:** `{PRIMARY}` · **Target cadence:** `{N}`/week (archetype template §3)
**Source material:** `{SOURCE_VIDEO_OR_ORIGINAL}` · **Shorts in this plan:** `{N}`

## 1. Strategy

| Field | Value |
|---|---|
| Cadence committed | `{N}`/week — sustainable at `{HOURS}`/week |
| What Shorts are for here | `{REACH / SEARCH_LONG_TAIL / AUDIENCE_TEST / FUNNEL}` |
| Relationship to long-form | Audience overlap is ~10% (`benchmarks.md` §8) — plan accordingly |
| Positioning on the vertical feed | `{HOW_A_STRANGER_RECOGNISES_THIS_CHANNEL_IN_3_SECONDS}` |
| Posting times | `{TIMES}` |

**Series concepts** — recurring formats a viewer can recognise and expect:

| Series | Premise | Repeatable because | Episodes available | Cover convention |
|---|---|---|---|---|
| `{NAME}` | `{PREMISE}` | `{REASON}` | `{N}` | `{CONVENTION}` |
| | | | | |

## 2. Short `{N}` — `{WORKING_NAME}`

*(repeat this block for each Short)*

| Field | Value |
|---|---|
| Source beat | `{TIMESTAMP_IN_LONG_FORM}` — `{WHAT_HAPPENS}` |
| Why this beat stands alone | `{REASON}` |
| Target length | `{N}`s — avoiding the 30–45s dead zone (`benchmarks.md` §8) |
| Series | `{SERIES_OR_STANDALONE}` |

**Hook (0–3s), spoken word for word:**
> "`{LINE}`"

**Script, timed:**

| Time | Spoken | On-screen text | Placement | Visual change |
|---|---|---|---|---|
| 0–3s | `{LINE}` | `{TEXT}` | `{TOP/CENTER/LOWER-THIRD}` | `{CUT/ZOOM/GRAPHIC}` |
| 3–6s | | | | |
| 6–9s | | | | |
| `{N}`–`{N}`s | | | | |

Safe zones: keep text clear of the top `{N}`% (title bar) and bottom `{N}`% (caption/UI).

**Loop design:** `{HOW_THE_LAST_FRAME_LEADS_BACK_INTO_THE_FIRST}`
Loop type: `{VISUAL_MATCH / SENTENCE_COMPLETION / QUESTION_REOPENED}`

| Field | Value |
|---|---|
| Cover frame | `{TIMESTAMP}` — `{WHY_THIS_FRAME}` |
| Title (≤40 chars) | `{TITLE}` — `{N}` chars |
| Hashtags (1–5, ≤60 chars) | `#{A} #{B} #{C}` — `{N}` chars |
| Description first 125 chars | `{TEXT}` |
| CTA | `{ONE_OR_NONE}` |

**Audio decision:** `{ORIGINAL_ONLY / 1 TRACK / 2+ TRACKS}`
**Revenue reasoning:** no music = creator keeps 100% of their share · 1 licensed track = 50%
· 2 tracks = 33% (`benchmarks.md` §8). Chosen because `{REASON}`.

## 3. Batch production

| Task | Shorts covered | When | Time |
|---|---|---|---|
| Clip selection | `{N}` | `{DATE}` | `{HOURS}` |
| Captioning | `{N}` | `{DATE}` | `{HOURS}` |
| Cover frames + titles | `{N}` | `{DATE}` | `{HOURS}` |

## 4. Publishing schedule

| Short | Date | Time | Series | Notes |
|---|---|---|---|---|
| `{N}` | `{DATE}` | `{TIME}` | `{SERIES}` | `{NOTE}` |
| | | | | |

## 5. What to measure

| Metric | Target | Emergency threshold |
|---|---|---|
| Completion rate | 70%+ triggers promotion | — |
| Viewed vs Swiped Away | 75%+ healthy | below 60% — rewrite the hook now |
| Loop / replay rate | >100% average viewed | — |

Never compare these three numbers to each other — they measure different things (`benchmarks.md` §8).

## Self-check

- [ ] Every Short is 9:16, 1080×1920, under 3 minutes (`benchmarks.md` §8)
- [ ] Lengths sit in the 15–60s sweet spot and avoid the 30–45s dead zone
- [ ] Hook lands inside the first 1–3 seconds, written as a spoken line
- [ ] A visual change occurs at least every 3 seconds throughout
- [ ] Every title is 40 characters or fewer
- [ ] Hashtags number 1–5 and total 60 characters or fewer
- [ ] On-screen text is inside the safe zones, clear of platform UI
- [ ] Loop design is explicit for every Short — not "it just ends"
- [ ] Audio decision states the revenue-split reasoning, not just a preference
- [ ] Cadence matches the archetype's `shorts_per_week` and the creator's real capacity
- [ ] No claim that Shorts convert to long-form beyond the ~10% overlap in `benchmarks.md` §8
