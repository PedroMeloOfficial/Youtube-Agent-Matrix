# Channel Profile

> Fill in OUTPUT LANGUAGE. Section headings may be translated.

> Owned by `channel-strategist`; read by every other agent. Unfilled fields stay `⟨TBD⟩` — never a plausible guess, never guessed.

## 1. Identity

| Field | Value |
|---|---|
| Channel name · handle | `{NAME}` · `{HANDLE}` |
| Output language | `{LANGUAGE}` |
| Market / country | `{MARKET}` |
| Host on camera | `{YES/NO/PARTIAL}` |
| Territory (the subject this channel owns) | `{TERRITORY}` |
| Size tier (`new`/`growing`/`established`/`authority`) | `{TIER}` |
| Subscribers · monthly views | `{SUBS}` · `{VIEWS}` |
| Primary archetype | `{PRIMARY}` |
| Secondary archetype (only if 25%+ of output) | `{SECONDARY_OR_NONE}` |
| Cadence — long-form · Shorts | `{LONG}` · `{SHORTS}` |

## 2. Positioning sentence

Required shape, one sentence, no clauses added:

> **I `{DO WHAT}` for `{FOR WHOM}`, because `{THE TENSION ONLY I RESOLVE}`.**

`{POSITIONING_SENTENCE}`
**Test:** if a named competitor could say this exact sentence, it is a **category**, not a
positioning — rewrite until only this channel can say it.
Competitor who could also say it: `{NAME_OR_NONE}` → verdict: `{POSITIONING / CATEGORY — REWRITE}`

## 3. Unfair advantage

What a well-funded competitor could not copy within a year.

| Advantage | Why it is not copyable | Where it shows up on screen |
|---|---|---|
| `{ADVANTAGE}` | `{WHY}` | `{WHERE}` |

## 4. Audience

| Question | Answer |
|---|---|
| What they already watch | `{ANSWER}` |
| What they want to **feel** | `{ANSWER}` |
| What makes them **leave** | `{ANSWER}` |
| What makes them **share** | `{ANSWER}` |

## 5. Content pillars

Jobs: `growth` · `authority` · `retention` · `differentiation`. Shares sum to 100%.

| # | Pillar | What qualifies | What disqualifies | Job | Monthly share |
|---|---|---|---|---|---|
| P1 | `{NAME}` | `{QUALIFIES}` | `{DISQUALIFIES}` | `{JOB}` | `{%}` |
| P2 | | | | | |
| P3 | | | | | |
| P4 | | | | | |
| P5 | | | | | |

## 6. Voice

| Is | Is not |
|---|---|
| `{TRAIT}` | `{TRAIT}` |

**Signature elements** (recurring phrases, structures, rituals, running bits):
`{ELEMENT}` — used `{WHEN}` · `{ELEMENT}` — used `{WHEN}`
**Never say / never do:** `{HARD_NOS}`

## 7. Formats

| Format | Pillar(s) | Length | Effort | Cadence | Status |
|---|---|---|---|---|---|
| `{FORMAT}` | `{P#}` | `{MIN}` | `{L/M/H}` | `{CADENCE}` | `{ACTIVE/TESTING/RETIRED}` |

## 8. Constraints

| Constraint | Detail |
|---|---|
| Production reality (hours/week, who does what) | `{DETAIL}` |
| Tooling — recording, editing, turnaround | `{DETAIL}` |
| Blackout windows (dates unavailable) | `{DATES}` |
| Rights limits (footage, music, NDAs) | `{DETAIL}` |

## 9. Live queue

| Slug | Working title | Pillar | Stage | Publish date |
|---|---|---|---|---|
| `{SLUG}` | `{TITLE}` | `{P#}` | `{STAGE}` | `{DATE}` |

## 10. Learnings log — append only, never edit or delete a row

| Date | What was tried | What happened | What it changes |
|---|---|---|---|
| `{DATE}` | `{ACTION}` | `{OUTCOME_WITH_NUMBERS}` | `{RULE_OR_NONE}` |

## Self-check

- [ ] Positioning uses the required shape and survives the competitor test
- [ ] Every pillar states what **disqualifies** a video, not only what qualifies
- [ ] Pillar shares sum to 100%
- [ ] Size tier matches subscriber count (`channel-types/_schema.md`, Channel size tier)
- [ ] Cadence compared against archetype template §3 and `benchmarks.md` §4
- [ ] Secondary archetype set only if it describes 25%+ of output
- [ ] Voice "is not" list is as long as the "is" list
- [ ] Constraints give real hours, dates and tooling — not "limited time"
- [ ] Every unfilled field is `⟨TBD⟩`, never a plausible guess
- [ ] No benchmark or revenue figure here that is absent from `benchmarks.md`
