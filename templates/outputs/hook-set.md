# Hook Set — `{WORKING_TITLE}`

> Fill in OUTPUT LANGUAGE. Section headings may be translated.

**Idea card:** `{PATH}` · **Archetype:** `{PRIMARY}` · **Target length:** `{MIN}` min
**Intended traffic source:** `{SEARCH / SUGGESTED / BROWSE}` — this decides which hooks fit.

Every line below is written as spoken. No greeting, no channel name, no "in this video".

---

## 1. Eight hook options

| # | Framework | The literal first line | Traffic fit | Risk |
|---|---|---|---|---|
| 1 | `{FRAMEWORK}` | "`{LINE}`" | `{SEARCH/BROWSE/SUGGESTED}` | `{RISK}` |
| 2 | | "" | | |
| 3 | | "" | | |
| 4 | | "" | | |
| 5 | | "" | | |
| 6 | | "" | | |
| 7 | | "" | | |
| 8 | | "" | | |

Frameworks are named from `references/hook-library.md`. At least **5** distinct frameworks
must appear — eight variations of one framework is one hook, not eight. These 8 are the shortlist
from the 10-hook generation drill (`hook-library.md` §8); the 3 ranked highest get their full
continuation written out in §2 and go back to the orchestrator.

---

## 2. Full openings — top 3

*(repeat this block for each of the top 3)*

### Option `{N}` — `{FRAMEWORK}`

| Window | Script (spoken, word for word) | Visual |
|---|---|---|
| **0–5s** | `{THE_HOOK_LINE}` | `{SHOT}` |
| **5–15s** | `{CONTEXT_AND_STAKES}` | `{SHOT}` |
| **15–30s** | `{PROMISE_AS_CAPABILITY + OPEN LOOP NAMED}` | `{SHOT}` |

**Open loop planted:** `{THE_QUESTION_LEFT_HANGING}` — paid off at `{TIMESTAMP}`
**Word count 0–30s:** `{N}` (budget = `0.5 × {RATE}`, `{RATE}` = speaking rate for `OUTPUT LANGUAGE`,
`references/localization-guide.md` §7 — ~140 wpm, so ~70 words, is the English baseline)
**Why it works:** `{ONE_LINE}`
**Where it could fail:** `{ONE_LINE}`

---

## 3. Anti-pattern check

| Anti-pattern | Present? | Where | Fixed? |
|---|---|---|---|
| Greeting / channel name / "welcome back" | `{Y/N}` | `{OPT#}` | `{Y/N}` |
| "In this video I'm going to…" | `{Y/N}` | | |
| Asking to like/subscribe before content | `{Y/N}` | | |
| Long logo or music intro | `{Y/N}` | | |
| Apology or throat-clearing ("sorry it's been a while") | `{Y/N}` | | |
| Topic stated instead of a promise | `{Y/N}` | | |
| Curiosity gap with no payoff planned | `{Y/N}` | | |
| Promise the video cannot actually deliver | `{Y/N}` | | |

Any `Y` unfixed blocks delivery.

---

## 4. Shorts variant

Vertical version of the strongest hook, compressed to the 1–3 second window.

| Field | Value |
|---|---|
| Hook line (0–3s) | "`{LINE}`" |
| On-screen text | `{TEXT}` (`{PLACEMENT}`) |
| What changes vs long-form | `{DIFFERENCE}` |
| Visual change cadence | every `{N}`s |
| Loop plan | `{HOW_THE_END_FEEDS_THE_START}` |

---

## 5. Ranking

| # | Option | Framework | Grab (1-5) | Deliverable (1-5) | On-brand (1-5) | Total |
|---|---|---|---|---|---|---|
| 1 | `{N}` | `{FRAMEWORK}` | | | | `{SUM}` |
| 2 | | | | | | |
| 3 | | | | | | |

> ### ▶ Recommended: option `{N}` — `{FRAMEWORK}`
> **Why:** `{ONE_LINE_REASON}`
> **Use option `{N}` instead if** `{CONDITION}`.

---

## Self-check

- [ ] Eight distinct options, drawing on at least `{N}` different frameworks from `hook-library.md`
- [ ] Every line written as it will be spoken, in OUTPUT LANGUAGE — not translated English idiom
- [ ] No option opens with a greeting, the channel name, or "in this video"
- [ ] Top 3 have full 0–5 / 5–15 / 15–30 second continuations with visuals
- [ ] The promise is a capability the viewer gains, not the topic
- [ ] An open loop is planted in the first 30s and its payoff timestamp is named
- [ ] Hook style matches the archetype's §6 primary/secondary frameworks
- [ ] 30s retention target from `benchmarks.md` §2 (first 60 seconds) noted and realistic
- [ ] Shorts variant respects the 1–3s hook window and 3s visual-change rule (`benchmarks.md` §8)
- [ ] Anti-pattern table has no unfixed `Y`
- [ ] One recommendation, with a reason and a switch condition
