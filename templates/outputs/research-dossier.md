# Research Dossier — `{TOPIC}`

> Fill in OUTPUT LANGUAGE. Section headings may be translated.

**Requested by:** `{STAGE}` · **Date:** `{DATE}` · **Web access:** `{YES/NO}`
Every downstream claim in the script must trace to a row in §1 or be marked `⚠️ verify`.

## 1. Verified facts

| # | Claim (one sentence) | Source | URL | Date | Confidence |
|---|---|---|---|---|---|
| F1 | `{CLAIM}` | `{PUBLISHER}` | `{URL}` | `{DATE}` | `{HIGH/MED/LOW}` |
| F2 | | | | | |
| F3 | | | | | |

**Could not verify** (do not say on camera): `{CLAIM}` — `{WHY_UNVERIFIABLE}`

## 2. Documented decisions

Things a person or organisation stated they chose to do, and why — separated from outcomes and
from interpretation.

| # | Who | What they decided | Stated reason | Where documented |
|---|---|---|---|---|
| D1 | `{WHO}` | `{DECISION}` | `{THEIR_OWN_WORDS}` | `{SOURCE_URL}` |
| | | | | |

## 3. Existing coverage

| Title (creator's language) | Title (English) | Channel | Angle | Length | Views | Date |
|---|---|---|---|---|---|---|
| `{TITLE_LOCAL}` | `{TITLE_EN}` | `{CHANNEL}` | `{ANGLE}` | `{MIN}` | `{VIEWS}` | `{DATE}` |
| | | | | | | |

**Saturation read:** `{SATURATED / CONTESTED / OPEN}` — `{ONE_LINE}`

## 4. Unoccupied angles

| Angle | Nobody covers it because | Verdict | Evidence for the verdict |
|---|---|---|---|
| `{ANGLE}` | `{REASON}` | `{REAL GAP / BAD IDEA}` | `{SEARCH_DEMAND_OR_COMMENT_EVIDENCE}` |
| | | | |

A `bad idea` row is a finding, not a failure — record it so it is not re-proposed.

## 5. The discourse

| Layer | What it is | Where it lives |
|---|---|---|
| **Consensus** | `{WHAT_EVERYONE_REPEATS}` | `{SOURCE}` |
| **Contrarian** | `{THE_CREDIBLE_MINORITY_VIEW}` | `{SOURCE}` |
| **Fixation** | `{THE_DETAIL_THE_AUDIENCE_KEEPS_RETURNING_TO}` | `{SOURCE}` |
| **Controversy** | `{THE_LIVE_DISPUTE}` — heat level `{H/M/L}` | `{SOURCE}` |

**Language the audience uses for this topic:** `{TERMS}`

## 6. Timing hook

| Question | Answer |
|---|---|
| Is there a dated reason to publish now? | `{YES / NO}` |
| If yes — the date and what happens | `{DATE}` — `{EVENT}` |
| Publish-by date before it goes stale | `{DATE}` |
| If no — say so plainly | `No timing hook. This is evergreen.` |

Never manufacture urgency. `NO` is a valid and common answer.

## 7. Obtainable b-roll

| Shot / footage | Where it exists | Licence / rights | Obtainable? | Substitute if not |
|---|---|---|---|---|
| `{SHOT}` | `{SOURCE}` | `{LICENCE}` | `{YES/NO}` | `{SUBSTITUTE}` |
| | | | | |

**Do not request** — the script must never call for these:

- `{FOOTAGE}` — `{WHY_NOT: rights / does not exist / paywalled}`
- `{FOOTAGE}` — `{WHY_NOT}`

## 8. Strongest findings

The three things that make this video worth making at all.

1. `{FINDING}` — sourced at F`{N}`
2. `{FINDING}` — sourced at F`{N}`
3. `{FINDING}` — sourced at F`{N}`

**Weakest link in the argument:** `{WHAT_A_CRITIC_WOULD_ATTACK}`

## 9. Sources

| # | Publisher | URL | Type | Reliability |
|---|---|---|---|---|
| S1 | `{PUBLISHER}` | `{URL}` | `{PRIMARY/SECONDARY/FORUM}` | `{A/B/C}` |
| | | | | |

## Self-check

- [ ] Every fact in §1 has a URL and a date — no unsourced rows
- [ ] Claims that could not be verified are listed as unverifiable, not quietly dropped
- [ ] Documented decisions kept separate from outcomes and from interpretation
- [ ] Existing-coverage titles given in both the creator's language and English
- [ ] Every unoccupied angle judged `real gap` or `bad idea` — none left unjudged
- [ ] Timing hook answered `yes-with-date` or `no` — never vague
- [ ] Every b-roll row states rights and an obtainability verdict; a "do not request" list exists
- [ ] Any platform statistic quoted traces to `benchmarks.md`, not to a source found while researching
- [ ] The weakest link in the argument is named
