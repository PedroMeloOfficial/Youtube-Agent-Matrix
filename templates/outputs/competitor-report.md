# Competitor Report — `{DATE}`

> Fill in OUTPUT LANGUAGE. Section headings may be translated.

**Our channel:** `{CHANNEL_NAME}` · `{TIER}` · `{PRIMARY_ARCHETYPE}` · `{MARKET}`
**Scope:** `{N}` competitors · `{DATE_RANGE}` · **Data source:** `{API / MANUAL / PASTED}`

## 1. Competitor set

| # | Channel | Subs | Uploads/mo | Median views | Archetype | Why in the set | Threat |
|---|---|---|---|---|---|---|---|
| C1 | `{NAME}` | `{SUBS}` | `{N}` | `{VIEWS}` | `{ARCHETYPE}` | `{REASON}` | `{H/M/L}` |
| C2 | | | | | | | |
| C3 | | | | | | | |
| C4 | | | | | | | |
| C5 | | | | | | | |

Excluded and why: `{CHANNEL}` — `{REASON}`

## 2. Outlier analysis — relative to each competitor's own baseline

An outlier is a video that beat **its own channel's** median, not ours. Absolute view counts
across channels of different sizes are not comparable.

*(repeat this block for each competitor)*

### C`{N}` — `{CHANNEL_NAME}` · baseline median: `{MEDIAN}` views

| Title | Views | × baseline | Format | Packaging move | Why it broke out |
|---|---|---|---|---|---|
| `{TITLE}` | `{VIEWS}` | `{X}×` | `{FORMAT}` | `{TITLE_OR_THUMB_PATTERN}` | `{HYPOTHESIS}` |
| | | | | | |

**Pattern across their outliers:** `{PATTERN}`
**Their misses (below 0.5× baseline) and what they share:** `{PATTERN}`

## 3. Keyword & topic gaps

| Term / topic | Who covers it | Their depth | Freshness | Our coverage | Gap type |
|---|---|---|---|---|---|
| `{TERM}` | `{C1, C3}` | `{THIN/DEEP}` | `{YEARS_OLD}` | `{NONE/PARTIAL}` | `{UNCOVERED / STALE / SHALLOW}` |
| | | | | | |

## 4. Format & packaging gaps

| Dimension | What the set does | What nobody does | Openable by us? |
|---|---|---|---|
| Video length | `{OBSERVED}` | `{ABSENT}` | `{YES/NO — WHY}` |
| Title pattern | `{OBSERVED}` | `{ABSENT}` | `{YES/NO}` |
| Thumbnail style | `{OBSERVED}` | `{ABSENT}` | `{YES/NO}` |
| Series / recurring format | `{OBSERVED}` | `{ABSENT}` | `{YES/NO}` |
| Shorts usage | `{OBSERVED}` | `{ABSENT}` | `{YES/NO}` |
| Production level | `{OBSERVED}` | `{ABSENT}` | `{YES/NO}` |

## 5. Audience gaps — from comment mining

Sampled `{N}` comments across `{M}` videos.

| Recurring request / complaint | Frequency | Which competitor's audience | Nobody answers it because |
|---|---|---|---|
| `{QUOTE_OR_THEME}` | `{N}×` | `{C2}` | `{REASON}` |
| | | | |

**Vocabulary the audience actually uses** (for SEO and titles): `{TERMS}`

## 6. Exploitable openings — ranked

For each: **why it's open** must be answered honestly — a real gap, or nobody wants it.

| # | Opening | Evidence it exists | Why it's open | Verdict | Effort | Owns it in |
|---|---|---|---|---|---|---|
| 1 | `{OPENING}` | `{EVIDENCE}` | `{REASON}` | `{REAL GAP / NOBODY WANTS IT}` | `{H/M/L}` | `{N} videos` |
| 2 | | | | | | |
| 3 | | | | | | |
| 4 | | | | | | |
| 5 | | | | | | |

**Recommended first move:** `{#N}` — `{ONE_LINE_REASON}`

**Openings rejected as "nobody wants it":** `{LIST}` — kept here so the same idea is not
re-proposed next quarter.

## 7. Where we are already ahead

| Dimension | Our advantage | Defensible? |
|---|---|---|
| `{DIMENSION}` | `{ADVANTAGE}` | `{YES/NO — WHY}` |
| | | |

## Self-check

- [ ] Outliers measured against each competitor's **own** median, never across channels
- [ ] Every competitor row states why it is in the set (same audience, not same topic)
- [ ] Every opening answers "real gap or nobody wants it" — no opening left unjudged
- [ ] Comment evidence is quoted or counted, not paraphrased as a general impression
- [ ] Any CTR / retention / cadence figure cited traces to `benchmarks.md` (§1, §2, §4)
- [ ] Openings ranked against **our** constraints in `channel-profile.md` §8, not in the abstract
- [ ] Public-data limits stated where numbers could not be verified
- [ ] Nothing recommended that our size tier cannot execute
