# Repurpose Plan — `{SOURCE_VIDEO_TITLE}`

> Fill in OUTPUT LANGUAGE. Section headings may be translated.

**Source:** `{SLUG}` · `{MM:SS}` · published `{DATE}`
**Production model:** `{SOLO / SMALL-TEAM}` · **Weekly hours available for repurposing:** `{N}`

A solo creator cannot do all of this. §5 exists so they know exactly what to skip.

## 1. Extractable beats

| # | Timestamp | Beat | Stands alone because | Best destination | Length |
|---|---|---|---|---|---|
| B1 | `{MM:SS}–{MM:SS}` | `{BEAT}` | `{REASON}` | `{PLATFORM}` | `{N}`s |
| B2 | | | | | |
| B3 | | | | | |
| B4 | | | | | |

**Beats deliberately not extracted:** `{B#}` — `{WHY: needs context / spoils the video / weak alone}`

## 2. Platform adaptations

| Platform | Format & spec | Hook adaptation | What changes | What stays | Beats used |
|---|---|---|---|---|---|
| YouTube Shorts | `{SPEC}` | `{ADAPTATION}` | `{CHANGES}` | `{STAYS}` | `{B#}` |
| `{PLATFORM_2}` | | | | | |
| `{PLATFORM_3}` | | | | | |
| `{PLATFORM_4}` | | | | | |
| `{PLATFORM_5}` | | | | | |
| Community tab | `{SPEC}` | `{ADAPTATION}` | | | |
| Newsletter / blog | `{SPEC}` | `{ADAPTATION}` | | | |

"What stays" is the point — the thesis and the voice must survive every adaptation, or the
repurposed asset stops building the same channel.

## 3. Publication schedule

Relative to the long-form upload (D-0).

| Day | Platform | Asset | Owner | Done |
|---|---|---|---|---|
| D-2 | `{PLATFORM}` | `{TEASER_ASSET}` | `{WHO}` | [ ] |
| D-0 | YouTube | Long-form publish | `{WHO}` | [ ] |
| D-0 | `{PLATFORM}` | `{ASSET}` | `{WHO}` | [ ] |
| D+1 | | | | [ ] |
| D+2 | | | | [ ] |
| D+4 | | | | [ ] |
| D+7 | | | | [ ] |
| D+14 | | | | [ ] |

Nothing competes with the long-form on D-0 for the same audience's attention.

## 4. Asset naming

Convention: `{SLUG}_{PLATFORM}_{BEAT}_{VERSION}.{EXT}`

| Asset | File name | Location |
|---|---|---|
| `{ASSET}` | `{FILENAME}` | `{PATH}` |
| | | |

Master files: `{PATH}` · Exports: `{PATH}`

## 5. Effort vs return — what to skip

| Rank | Asset | Effort | Expected return | Ratio | Verdict |
|---|---|---|---|---|---|
| 1 | `{ASSET}` | `{MIN}` | `{RETURN}` | `{HIGH}` | **Do first** |
| 2 | | | | | Do |
| 3 | | | | | Do if time |
| 4 | | | | | **Skip** |
| 5 | | | | | **Skip** |

> **If you only have `{N}` minutes this week, do rank 1 and stop.**
> Skipping ranks 4–5 costs `{WHAT_IS_LOST}` — an acceptable loss because `{REASON}`.

## 6. Rights and watermarks

| Flag | Applies to | Detail | Action |
|---|---|---|---|
| Third-party footage | `{ASSETS}` | `{LICENCE}` | `{REDISTRIBUTABLE? YES/NO}` |
| Licensed music | `{ASSETS}` | `{TRACK}` | `{STRIP / REPLACE / KEEP}` |
| Platform watermark | `{ASSETS}` | `{PLATFORM}` | Re-export from master, never re-download |
| Guest / interviewee consent | `{ASSETS}` | `{SCOPE_OF_CONSENT}` | `{OK / ASK_FIRST}` |
| Sponsor obligations | `{ASSETS}` | `{CONTRACT_TERMS}` | `{DISCLOSE / EXCLUDE}` |

Any `NO` or `ASK_FIRST` blocks that asset until resolved.

## Self-check

- [ ] Every beat states why it stands alone without the surrounding video
- [ ] Each platform row specifies a real format spec, not "short clip"
- [ ] Hook is re-written per platform — never copied across unchanged
- [ ] "What stays" preserves the thesis and voice on every platform
- [ ] Shorts specs match `benchmarks.md` §8 (length, ratio, hook window, title limit)
- [ ] Schedule is day-by-day relative to upload and nothing collides with D-0
- [ ] Asset naming convention is applied consistently to every file listed
- [ ] Effort/return ranking exists and explicitly names what to **skip**
- [ ] The skip recommendation states what is lost and why that is acceptable
- [ ] Every rights flag resolved or the asset is blocked
- [ ] Re-exports come from masters — no platform-watermarked re-uploads
